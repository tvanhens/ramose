{
  "name": "ramose-monorepo",
  "version": "0.3.0",
  "private": true,
  "description": "Development workspace for Ramose. The published package is `packages/ramose` (npm: `ramose`); this manifest is private and never ships.",
  "license": "Apache-2.0",
  "author": "Tyler van Hensbergen <tvanhens@gmail.com>",
  "homepage": "https://ramose.ai",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tvanhens/ramose.git"
  },
  "type": "module",
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "build": "bun run scripts/build-packages.ts --clean",
    "release": "bun run scripts/release.ts --no-provenance",
    "release:dry": "bun run scripts/release.ts --dry-run --no-provenance",
    "release:version": "bun run scripts/set-version.ts",
    "release:check": "bun run scripts/check-release.ts --built",
    "record:frames": "bun run scripts/record-optimistic-fence-frames.ts",
    "test:unit": "bun test --parallel=3 packages/ramose test/support website/scripts examples/reef/test",
    "test:browser": "bunx vitest run --config vitest.browser.config.ts",
    "test:conformance": "CI=${CI:-1} ALCHEMY_STATE=${ALCHEMY_STATE:-local} CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID:-0123456789abcdef0123456789abcdef} CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-x} bun test --parallel=1 test/local/integration.test.ts",
    "test:local": "bun run test:conformance",
    "test": "bun run test:unit && bun run test:local",
    "test:all": "bun run test && bun run test:e2e",
    "test:e2e": "bun test test/e2e",
    "test:e2e:cf": "bash scripts/e2e-cloudflare.sh",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json",
    "bench": "bun run bench/run.ts",
    "bench:seek": "bun run bench/seek.bench.ts",
    "bench:join": "bun run bench/join.bench.ts",
    "bench:write": "bun run bench/write.bench.ts",
    "bench:transactor": "bun run bench/transactor.bench.ts",
    "bench:write-do": "bun run bench/write-do.bench.ts",
    "bench:read-do": "bun run bench/read-do.bench.ts",
    "bench:indexer": "bun run bench/indexer.bench.ts",
    "dev": "bun run scripts/build-packages.ts && bun alchemy dev",
    "dev:todos": "bun run scripts/build-packages.ts && CI=${CI:-1} ALCHEMY_STATE=${ALCHEMY_STATE:-local} CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID:-0123456789abcdef0123456789abcdef} CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-x} bun alchemy dev examples/todos/alchemy.run.ts",
    "dev:reef": "bun run scripts/build-packages.ts && CI=${CI:-1} ALCHEMY_STATE=${ALCHEMY_STATE:-local} CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID:-0123456789abcdef0123456789abcdef} CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-x} bun alchemy dev examples/reef/alchemy.run.ts",
    "dev:reef:ui": "bun run examples/reef/dev.ts",
    "build:reef": "bun run scripts/build-packages.ts && bun build examples/reef/index.html --outdir examples/reef/dist --minify",
    "dev:graph": "bun run scripts/build-packages.ts && CI=${CI:-1} ALCHEMY_STATE=${ALCHEMY_STATE:-local} CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID:-0123456789abcdef0123456789abcdef} CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-x} bun alchemy dev examples/graph/alchemy.run.ts",
    "deploy": "bun run scripts/build-packages.ts && bun alchemy deploy",
    "deploy:prod": "bun run scripts/build-packages.ts && bun alchemy deploy --stage prod",
    "deploy:docs": "bun alchemy deploy website/alchemy.run.ts",
    "deploy:docs:prod": "bun alchemy deploy website/alchemy.run.ts --stage prod --adopt",
    "destroy": "bun alchemy destroy",
    "prepare": "effect-tsgo patch --typescript --no-oxlint"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "@effect/tsgo": "0.37.0",
    "@types/bun": "latest",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitest/browser-playwright": "4.1.10",
    "playwright": "1.62.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "typescript": "^7.0.2",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "@alchemy.run/better-auth": "^2.0.0-beta.74",
    "@effect/platform-bun": ">=4.0.0-rc.110",
    "@effect/platform-node": ">=4.0.0-rc.110",
    "alchemy": "2.0.0-beta.74",
    "better-auth": "^1.6.30",
    "effect": ">=4.0.0-rc.110",
    "jose": "^6.0.0",
    "ramose": "workspace:*"
  }
}
