---
title: Ramose — the database optimized for humans and agents
description: One offline-first graph database for your app and its agents, with one model, one operation surface, and one policy.
template: splash
head:
  - tag: title
    content: Ramose — the database optimized for humans and agents
  - tag: meta
    attrs:
      property: og:type
      content: website
---
import HeroMesh from "../../components/HeroMesh.astro";
import FeatureConsole from "../../components/FeatureConsole.astro";
import InstantSyncDemo from "../../components/InstantSyncDemo.astro";
import AgentCommandDemo from "../../components/AgentCommandDemo.astro";
import HistoryRecoveryDemo from "../../components/HistoryRecoveryDemo.astro";

<section class="rg-proto-hero">
  <HeroMesh />
  <h1>The database optimized for <em>humans</em> and <em>agents</em>.</h1>
  <div class="rg-proto-hero-grid">
    <p class="rg-proto-intro">Right now those are two jobs: the app you shipped, and the MCP server, auth path, and permission model you bolt on beside it for agents. Ramose makes them one. Declare graphs, schema, operations, and policy once—your UI gets offline-first writes and local queries, your agents get one discoverable MCP endpoint, and both use the same capabilities.</p>
    <div class="rg-proto-hero-cta">
      <div class="rg-proto-actions">
        <a class="rg-proto-button primary" href="/getting-started/quickstart/">Start in 15 minutes →</a>
        <a class="rg-proto-button secondary" href="#mcp">See the MCP path</a>
      </div>
      <div class="rg-proto-proof">
        <span>◆ Nothing to keep in sync</span>
        <span>◆ Works offline</span>
        <span>◆ No SQL to inject</span>
        <span>◆ Your Cloudflare account</span>
      </div>
    </div>
  </div>
</section>

<section class="rg-feature-console" aria-label="Ramose capabilities" data-feature-console>
  <div class="rg-feature-code">
    <div class="rg-feature-sample active" data-feature-sample>
      <p class="rg-console-label">schema.ts</p>

```ts
const Project = Ramose.Entity("project", {
  fields: { slug: Ramose.string().unique() },
  traits: [Ramose.Graph(ProjectCatalog)],
})
```

    </div>
    <div class="rg-feature-sample" data-feature-sample aria-hidden="true">
      <p class="rg-console-label">TaskList.tsx</p>

```tsx
const tasks = useQuery(
  projectDb.query
    .from(Task)
    .where(Task.done.eq(false)),
)
```

    </div>
    <div class="rg-feature-sample" data-feature-sample aria-hidden="true">
      <p class="rg-console-label">policy.ts</p>

```ts
const grants = [
  P.read(Task).when(isMember),
  P.invoke(Task.setDone).when(isMember),
]
```

    </div>
    <div class="rg-feature-sample" data-feature-sample aria-hidden="true">
      <p class="rg-console-label">agent.ts</p>

```ts
const openTasks = await mcp.callTool({
  name: "query",
  arguments: {
    at: ["acme", "launch"],
    query: openTasksDocument,
  },
})
```

    </div>
  </div>
  <div class="rg-feature-output">
    <div class="rg-feature-tabs" role="tablist" aria-label="Ramose capabilities">
      <button class="active" type="button" role="tab" aria-selected="true" aria-controls="feature-typed" data-feature-tab>Typed client</button>
      <button type="button" role="tab" aria-selected="false" aria-controls="feature-live" tabindex="-1" data-feature-tab>Live queries</button>
      <button type="button" role="tab" aria-selected="false" aria-controls="feature-permissions" tabindex="-1" data-feature-tab>Permissions</button>
      <button type="button" role="tab" aria-selected="false" aria-controls="feature-mcp" tabindex="-1" data-feature-tab>MCP endpoint</button>
    </div>
    <div class="rg-feature-cards">
      <div class="rg-feature-card active" id="feature-typed" role="tabpanel" data-feature-card>
        <p class="rg-console-label">Typed client</p>
        <h2>Type-safe reads and writes</h2>
        <p>Ask for a field that does not exist, or write the wrong shape, and TypeScript stops you before production does.</p>
        <div class="rg-feature-result">
          <span>You get</span>
          <code>useQuery(projectDb.query.from(Task))</code>
        </div>
      </div>
      <div class="rg-feature-card" id="feature-live" role="tabpanel" data-feature-card aria-hidden="true">
        <p class="rg-console-label">Live queries</p>
        <h2>Every open view keeps itself current</h2>
        <p>The query is the subscription. Local writes render immediately, then every connected client converges without cache keys or refetch calls.</p>
        <div class="rg-feature-result">
          <span>One query</span>
          <code>Task.setDone(&#123; done: true &#125;)</code>
        </div>
      </div>
      <div class="rg-feature-card" id="feature-permissions" role="tabpanel" data-feature-card aria-hidden="true">
        <p class="rg-console-label">Permissions</p>
        <h2>One policy for every caller</h2>
        <p>Field-level rules are enforced by the Ramose server for your UI and your agents, so neither path can quietly see more than the other.</p>
        <div class="rg-feature-result">
          <span>Enforced rule</span>
          <code>Task.setDone → member only</code>
        </div>
      </div>
      <div class="rg-feature-card" id="feature-mcp" role="tabpanel" data-feature-card aria-hidden="true">
        <p class="rg-console-label">MCP endpoint</p>
        <h2>Three tools understand your app</h2>
        <p>Agents discover visible entities, graphs, and operations progressively, then query or mutate through the same model your application already uses.</p>
        <div class="rg-feature-result">
          <span>Fixed tools</span>
          <code>describe · query · mutate</code>
        </div>
      </div>
    </div>
    <div class="rg-feature-progress"><i data-feature-progress></i><span data-feature-counter>1 / 4</span></div>
  </div>
  <FeatureConsole />
</section>

<section class="rg-proto-section rg-instant">
  <p class="rg-proto-kicker">01 — Instant by default</p>
  <h2>That snappy, no-spinner feel? You get it on day one.</h2>
  <p class="rg-proto-lede">The apps people call fast never wait on a round trip. Ramose gives you that architecture by default: writes commit to the local store and render immediately, sync catches up behind them, and every other client and agent converges on its own. Pull the plug; reads and writes keep working.</p>
  <InstantSyncDemo />
</section>

<section id="mcp" class="rg-proto-section rg-mcp">
  <div class="rg-proto-copy">
    <p class="rg-proto-kicker">02 — Agents</p>
    <h2>Everything a user can do an agent can do.</h2>
    <p class="rg-proto-lede">Ramose projects the capabilities already in your app through three MCP tools. Agents discover the graph and catalog progressively, query visible data, and invoke the same named operations as your UI. No shadow database, glue server, or second authorization model to keep aligned.</p>
  </div>
  <AgentCommandDemo />
</section>

<section class="rg-proto-section rg-history">
  <div class="rg-proto-copy">
    <p class="rg-proto-kicker">03 — History</p>
    <h2>Data is never lost.</h2>
    <p class="rg-proto-lede">Mistakes happen. A person clicks the wrong thing, an agent runs the wrong command, or a bug reaches production. Ramose keeps every committed version, so recovery is an elegant rewind instead of a restore drill. Move back to the moment before the mistake, verify the state, and continue.</p>
    <p class="rg-proto-link"><a href="/concepts/time-travel/">Read about time travel →</a></p>
  </div>
  <HistoryRecoveryDemo />
</section>

<section class="rg-proto-section rg-cloudflare">
  <div class="rg-proto-copy">
    <p class="rg-proto-kicker">04 — Backends</p>
    <h2>Cloudflare today, your provider tomorrow</h2>
    <p class="rg-proto-lede">Ramose runs on Cloudflare today, but your application is defined against Ramose — not a provider console. AWS and self-hosted backends are planned behind the same schema, policy, sync, and MCP surface, so changing infrastructure does not mean rewriting the app.</p>
    <p class="rg-proto-link"><a href="/guides/deploy/">See the deployment →</a></p>
  </div>
  <div class="rg-stack-card rg-provider-card">
    <div class="rg-provider-row implemented">
      <div><strong>Cloudflare</strong><code>Workers · Durable Objects · R2 · Analytics Engine</code></div>
      <span>Implemented</span>
    </div>
    <div class="rg-provider-row">
      <div><strong>AWS</strong><code>Lambda · DynamoDB · S3</code></div>
      <span>Planned</span>
    </div>
    <div class="rg-provider-row">
      <div><strong>Self-hosted</strong><code>Your compute · your object store · your network</code></div>
      <span>Planned</span>
    </div>
  </div>
</section>

<section class="rg-proto-close">
  <p class="rg-proto-kicker">Apache-2.0 · self-hosted · pre-release</p>
  <h2>Read the source. Run it in your own account.</h2>
  <p>One npm package, one Alchemy stack, no managed service in the path between your app and your data.</p>
  <div class="rg-proto-actions">
    <a class="rg-proto-button primary" href="/getting-started/quickstart/">Quickstart →</a>
    <a class="rg-proto-button secondary" href="https://github.com/tvanhens/ramose">GitHub</a>
  </div>
  <div class="rg-command-row"><code>bun add ramose</code><code>bun alchemy dev</code><code>bun alchemy deploy</code></div>
</section>

<nav class="rg-proto-footer" aria-label="Documentation" data-pagefind-ignore>
  <span>ramose.ai — the offline-first database your agents can call</span>
  <span><a href="/getting-started/introduction/">Docs</a> · <a href="/concepts/mental-model/">Mental model</a> · <a href="/getting-started/compare/">Compare</a></span>
</nav>
