<figure class="rg-agent-demo">
  <iframe
    src="/demos/agents-everything-a-user-can-do.html"
    title="An agent discovering, querying, and mutating through Ramose"
    loading="lazy"
    sandbox="allow-scripts"
  ></iframe>
</figure>

<style>
  .rg-agent-demo {
    container-type: inline-size;
    min-width: 0;
    margin: 0;
    overflow: hidden;
    border: 1px solid rgba(255, 101, 0, 0.26);
    border-radius: 0.9rem;
    background: #0a0908;
    box-shadow: 0 2.5rem 6rem -4rem rgba(255, 76, 0, 0.65);
  }

  .rg-agent-demo iframe {
    display: block;
    width: 100%;
    aspect-ratio: 40 / 52.65;
    border: 0;
  }

  @media (max-width: 56rem) {
    .rg-agent-demo {
      box-sizing: border-box;
      padding: 0.45rem;
    }

    .rg-agent-demo iframe {
      aspect-ratio: 40 / 50.25;
    }
  }

  @container (max-width: 35rem) {
    .rg-agent-demo iframe {
      height: calc(107.3cqw + 2.8125rem);
      aspect-ratio: auto;
    }
  }
</style>
