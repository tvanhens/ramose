<figure class="rg-sync-demo">
  <div class="rg-sync-scene" aria-hidden="true">
    <div class="rg-sync-browser">
      <div class="rg-sync-windowbar">
        <span class="rg-sync-window-dots"><i></i><i></i><i></i></span>
        <span class="rg-sync-address">app.ramose.local / board</span>
        <span class="rg-sync-live"><i></i> Live</span>
      </div>
      <div class="rg-sync-board">
        <div class="rg-sync-board-heading">
          <div>
            <span>Product</span>
            <strong>Launch board</strong>
          </div>
          <span class="rg-sync-presence"><i></i><i></i><i></i> 3 online</span>
        </div>
        <div class="rg-sync-columns">
          <section class="rg-sync-column">
            <header><span>Backlog</span><b>2</b></header>
            <div class="rg-sync-mini-card"><i></i><span>Write migration guide</span></div>
            <div class="rg-sync-mini-card muted"><i></i><span>Billing states</span></div>
          </section>
          <section class="rg-sync-column doing">
            <header><span>In progress</span><b class="rg-sync-changing-count"><i>1</i><i>0</i></b></header>
            <div class="rg-sync-drop-slot"></div>
          </section>
          <section class="rg-sync-column done">
            <header><span>Done</span><b class="rg-sync-changing-count"><i>1</i><i>2</i></b></header>
            <div class="rg-sync-mini-card complete"><i></i><span>Connect auth</span></div>
            <div class="rg-sync-drop-slot target"></div>
          </section>
          <div class="rg-sync-task">
            <span class="rg-sync-task-mark">R</span>
            <span><strong>Polish onboarding</strong><small>Launch · Ada</small></span>
            <b>•••</b>
          </div>
          <span class="rg-sync-local-write">Local write · 0ms</span>
        </div>
      </div>
    </div>

    <div class="rg-sync-transfer">
      <span class="rg-sync-link"><i></i></span>
      <svg class="rg-sync-mobile-link" viewBox="0 0 90 48" preserveAspectRatio="none" aria-hidden="true">
        <path class="track" d="M 1 0 V 47 H 90" pathLength="1"></path>
        <path class="packet" d="M 1 0 V 47 H 90" pathLength="1"></path>
      </svg>
      <small>sync</small>
    </div>

    <div class="rg-sync-phone">
      <div class="rg-sync-phone-shell">
        <span class="rg-sync-island"></span>
        <div class="rg-sync-phone-top">
          <span>9:41</span><span>● ◒</span>
        </div>
        <div class="rg-sync-phone-app">
          <div class="rg-sync-mobile-title">
            <div><span>Product</span><strong>Launch board</strong></div>
            <b>R</b>
          </div>
          <div class="rg-sync-mobile-filter"><span>My work</span><span class="active">All</span></div>
          <div class="rg-sync-mobile-list">
            <div class="rg-sync-mobile-row">
              <i class="orange"></i>
              <span><strong>Write migration guide</strong><small>Backlog</small></span>
            </div>
            <div class="rg-sync-mobile-row changing">
              <i></i>
              <span><strong>Polish onboarding</strong><small class="rg-sync-mobile-state"><em>In progress</em><em>Done</em></small></span>
              <b>✓</b>
            </div>
            <div class="rg-sync-mobile-row">
              <i class="green"></i>
              <span><strong>Connect auth</strong><small>Done</small></span>
            </div>
          </div>
          <div class="rg-sync-phone-status"><i></i><span>Synced just now</span></div>
        </div>
        <span class="rg-sync-home"></span>
      </div>
    </div>
  </div>
  <figcaption>
    <span>One drag. Two devices. No round trip.</span>
    <code>local write → 2 views updated</code>
  </figcaption>
</figure>

<style>
  .rg-sync-demo {
    --sync-cycle: 8s;
    margin: 2.25rem 0 0;
  }

  .rg-sync-scene {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1fr) clamp(2.5rem, 5vw, 4.75rem) clamp(11rem, 18vw, 13.5rem);
    align-items: center;
    min-height: 30rem;
    padding: clamp(1rem, 3vw, 2rem);
    overflow: hidden;
    border: 1px solid rgba(255, 145, 87, 0.2);
    border-radius: 1rem;
    background:
      radial-gradient(circle at 75% 45%, rgba(255, 83, 8, 0.095), transparent 24rem),
      linear-gradient(rgba(255, 255, 255, 0.018) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.018) 1px, transparent 1px),
      #090807;
    background-size: auto, 3rem 3rem, 3rem 3rem, auto;
    box-shadow: inset 0 1px rgba(255, 255, 255, 0.025), 0 2rem 6rem -4rem rgba(255, 76, 0, 0.6);
  }

  .rg-sync-browser {
    min-width: 0;
    overflow: hidden;
    border: 1px solid #342a25;
    border-radius: 0.8rem;
    background: #0e0c0b;
    box-shadow: 0 1.5rem 4rem -2.2rem #000;
  }

  .rg-sync-windowbar {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 0.75rem;
    min-height: 2.55rem;
    padding: 0 0.8rem;
    color: #70645d;
    border-bottom: 1px solid #29231f;
    background: #12100e;
    font-family: var(--sl-font-mono);
    font-size: 0.56rem;
  }

  .rg-sync-window-dots { display: flex; align-items: center; gap: 0.3rem; line-height: 1; }
  .rg-sync-window-dots i { width: 0.38rem; height: 0.38rem; margin: 0; border-radius: 50%; background: #4b403a; }
  .rg-sync-window-dots i:first-child { background: #a3411c; }
  .rg-sync-address { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; text-align: center; }
  .rg-sync-live { display: flex; align-items: center; gap: 0.32rem; color: #a49890; }
  .rg-sync-live i { width: 0.36rem; height: 0.36rem; border-radius: 50%; background: #ff5b0a; box-shadow: 0 0 0.7rem #ff4a00; }

  .rg-sync-board { padding: clamp(0.8rem, 2vw, 1.35rem); }
  .rg-sync-board-heading { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
  .rg-sync-board-heading > div { display: grid; gap: 0.15rem; }
  .rg-sync-board-heading span { color: #756961; font-family: var(--sl-font-mono); font-size: 0.54rem; text-transform: uppercase; letter-spacing: 0.1em; }
  .rg-sync-board-heading strong { color: #eee8e3; font-size: clamp(0.9rem, 1.8vw, 1.15rem); }
  .rg-sync-presence { display: flex; align-items: center; min-height: 1rem; line-height: 1; }
  .rg-sync-presence i { width: 1rem; height: 1rem; margin: 0 -0.3rem 0 0; border: 2px solid #0e0c0b; border-radius: 50%; background: #51443c; }
  .rg-sync-presence i:nth-child(2) { background: #7e3c20; }
  .rg-sync-presence i:nth-child(3) { margin: 0 0.5rem 0 0; background: #3c4c47; }

  .rg-sync-columns { position: relative; display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 0.55rem; }
  .rg-sync-column { min-width: 0; min-height: 14.5rem; padding: 0.58rem; border: 1px solid #29231f; border-radius: 0.55rem; background: rgba(255,255,255,0.012); }
  .rg-sync-column header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 0.6rem; color: #8b7e76; font-family: var(--sl-font-mono); font-size: 0.55rem; text-transform: uppercase; letter-spacing: 0.06em; }
  .rg-sync-column header b { display: grid; place-items: center; width: 1.15rem; height: 1.15rem; overflow: hidden; color: #655952; border-radius: 50%; background: #1b1714; font-size: 0.5rem; }
  .rg-sync-mini-card { display: flex; align-items: center; gap: 0.45rem; min-height: 2.85rem; margin-top: 0.42rem; padding: 0.52rem; color: #bdb2ab; border: 1px solid #302824; border-radius: 0.42rem; background: #151210; font-size: 0.62rem; line-height: 1.3; }
  .rg-sync-mini-card i { flex: 0 0 auto; width: 0.4rem; height: 0.4rem; border-radius: 50%; background: #66584f; }
  .rg-sync-mini-card.muted { opacity: 0.55; }
  .rg-sync-mini-card.complete i { background: #477065; }
  .rg-sync-drop-slot { height: 4.25rem; margin: 0; border: 1px dashed #332a26; border-radius: 0.45rem; }
  .rg-sync-column.doing .rg-sync-drop-slot { animation: rg-sync-source-slot var(--sync-cycle) ease-in-out infinite; }
  .rg-sync-drop-slot.target { margin-top: 0.42rem; animation: rg-sync-target-slot var(--sync-cycle) ease-in-out infinite; }

  .rg-sync-task {
    position: absolute;
    z-index: 3;
    top: 3.79rem;
    left: calc(33.333% + 0.825rem);
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 0.5rem;
    width: calc(33.333% - 1.65rem);
    min-height: 4.25rem;
    margin: 0;
    padding: 0.6rem;
    border: 1px solid #5a3525;
    border-radius: 0.45rem;
    background: linear-gradient(145deg, #211610, #171210);
    box-shadow: 0 0.8rem 1.8rem -1.4rem #000;
    animation: rg-sync-drag var(--sync-cycle) cubic-bezier(.36,.65,.24,1) infinite;
  }

  .rg-sync-task-mark { display: grid; place-items: center; width: 1.35rem; height: 1.35rem; color: #1b0c05; border-radius: 0.34rem; background: #ff5b0a; font-family: var(--sl-font-mono); font-size: 0.58rem; font-weight: 800; }
  .rg-sync-task > span:nth-child(2) { display: grid; min-width: 0; gap: 0.18rem; }
  .rg-sync-task strong { overflow: hidden; color: #f0eae5; font-size: 0.66rem; text-overflow: ellipsis; white-space: nowrap; }
  .rg-sync-task small { color: #766961; font-family: var(--sl-font-mono); font-size: 0.48rem; }
  .rg-sync-task > b { color: #645850; font-size: 0.55rem; letter-spacing: 0.06em; }

  .rg-sync-local-write { position: absolute; z-index: 4; right: 0.75rem; top: 6.9rem; padding: 0.35rem 0.5rem; color: #ffb284; border: 1px solid rgba(255,91,10,.32); border-radius: 999px; background: #1b100b; font-family: var(--sl-font-mono); font-size: 0.48rem; opacity: 0; animation: rg-sync-local var(--sync-cycle) ease infinite; }
  .rg-sync-changing-count { position: relative; }
  .rg-sync-changing-count i { position: absolute; margin: 0; font-style: normal; animation: rg-sync-count-a var(--sync-cycle) step-end infinite; }
  .rg-sync-changing-count i:last-child { opacity: 0; animation-name: rg-sync-count-b; }

  .rg-sync-transfer { display: grid; place-items: center; gap: 0.4rem; }
  .rg-sync-transfer small { color: #5f534c; font-family: var(--sl-font-mono); font-size: 0.48rem; text-transform: uppercase; letter-spacing: 0.12em; }
  .rg-sync-link { position: relative; display: block; width: 100%; height: 1px; background: linear-gradient(90deg, #36241b, #61301c, #36241b); }
  .rg-sync-link i { position: absolute; top: 50%; left: 0; width: 0.42rem; height: 0.42rem; border-radius: 50%; background: #ff5b0a; box-shadow: 0 0 0.6rem #ff3d00, 0 0 1.25rem rgba(255,61,0,.7); opacity: 0; transform: translate(-50%, -50%); animation: rg-sync-packet var(--sync-cycle) ease-in-out infinite; }
  .rg-sync-mobile-link { display: none; }

  .rg-sync-phone { position: relative; }
  .rg-sync-phone-shell { position: relative; min-height: 25.5rem; padding: 0.45rem; overflow: hidden; border: 1px solid #55443b; border-radius: 2rem; background: #050505; box-shadow: 0 1.8rem 3.5rem -2rem #000, 0 0 2.8rem -2rem rgba(255,76,0,.55); animation: rg-sync-phone-glow var(--sync-cycle) ease infinite; }
  .rg-sync-island { position: absolute; z-index: 2; top: 0.72rem; left: 50%; width: 3.35rem; height: 0.8rem; border-radius: 999px; background: #020202; transform: translateX(-50%); }
  .rg-sync-phone-top { display: flex; justify-content: space-between; padding: 0.55rem 0.7rem 0.5rem; color: #887b73; font-family: var(--sl-font-mono); font-size: 0.42rem; }
  .rg-sync-phone-app { padding: 0.7rem; }
  .rg-sync-mobile-title { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem; }
  .rg-sync-mobile-title > div { display: grid; gap: 0.12rem; }
  .rg-sync-mobile-title span { color: #6f635c; font-family: var(--sl-font-mono); font-size: 0.45rem; text-transform: uppercase; letter-spacing: .08em; }
  .rg-sync-mobile-title strong { color: #eee8e3; font-size: 0.78rem; }
  .rg-sync-mobile-title > b { display:grid; place-items:center; width:1.45rem; height:1.45rem; color:#1b0c05; border-radius:.38rem; background:#ff5b0a; font-family:var(--sl-font-mono); font-size:.58rem; }
  .rg-sync-mobile-filter { display: flex; gap: 0.35rem; margin-bottom: 0.75rem; color: #6d615a; font-family: var(--sl-font-mono); font-size: 0.45rem; }
  .rg-sync-mobile-filter span { padding: 0.28rem 0.45rem; border: 1px solid #2d2723; border-radius: 999px; }
  .rg-sync-mobile-filter .active { color: #ff9a61; border-color: #6e351d; background: #1a100b; }
  .rg-sync-mobile-list { display: grid; gap: 0.24rem; }
  .rg-sync-mobile-row { display: grid; grid-template-columns: auto minmax(0,1fr) auto; align-items: center; gap: 0.5rem; min-height: 3.1rem; margin: 0; padding: 0.5rem 0.55rem; border: 1px solid #2b2521; border-radius: 0.52rem; background: #12100e; }
  .rg-sync-mobile-row > i { width: 0.42rem; height: 0.42rem; border-radius: 50%; background: #ff5b0a; }
  .rg-sync-mobile-row > i.orange { background: #85502f; }
  .rg-sync-mobile-row > i.green { background: #477065; }
  .rg-sync-mobile-row > span { display: grid; min-width: 0; gap: 0.18rem; }
  .rg-sync-mobile-row strong { overflow: hidden; color: #c9bfb8; font-size: 0.56rem; white-space: nowrap; text-overflow: ellipsis; }
  .rg-sync-mobile-row small { color: #665a53; font-family: var(--sl-font-mono); font-size: 0.43rem; }
  .rg-sync-mobile-row > b { color: #ff7b39; opacity: 0; animation: rg-sync-check var(--sync-cycle) ease infinite; }
  .rg-sync-mobile-row.changing { animation: rg-sync-row var(--sync-cycle) ease infinite; }
  .rg-sync-mobile-state { display: grid; }
  .rg-sync-mobile-state em { grid-area: 1 / 1; font-style: normal; animation: rg-sync-state-a var(--sync-cycle) step-end infinite; }
  .rg-sync-mobile-state em:last-child { color: #6e9f91; opacity: 0; animation-name: rg-sync-state-b; }
  .rg-sync-phone-status { display: flex; justify-content: center; align-items: center; gap: 0.36rem; margin-top: 1rem; color: #6e625a; font-family: var(--sl-font-mono); font-size: 0.45rem; opacity: .55; animation: rg-sync-status var(--sync-cycle) ease infinite; }
  .rg-sync-phone-status i { width: .35rem; height: .35rem; border-radius:50%; background:#ff5b0a; }
  .rg-sync-home { position:absolute; bottom:.52rem; left:50%; width:3.5rem; height:.18rem; border-radius:999px; background:#4b433e; transform:translateX(-50%); }

  .rg-sync-demo figcaption { display: flex; justify-content: space-between; gap: 1rem; margin-top: 0.8rem; color: #8a7c74; font-size: 0.78rem; }
  .rg-sync-demo figcaption code { color: #a76846; background: transparent; font-size: 0.65rem; }

  @keyframes rg-sync-drag {
    0%, 17% { opacity:1; transform: translate3d(0,0,0) scale(1); box-shadow: 0 .8rem 1.8rem -1.4rem #000; }
    23% { opacity:1; transform: translate3d(0,-.35rem,0) scale(1.025); box-shadow: 0 1.5rem 2.2rem -1rem rgba(0,0,0,.9); }
    36%, 80% { opacity:1; transform: translate3d(calc(100% + 1.83rem),2.87rem,0) scale(1); border-color: #8c4828; box-shadow: 0 0 1.8rem -.8rem rgba(255,77,8,.55); }
    86% { opacity: 0; transform: translate3d(calc(100% + 1.83rem),2.87rem,0) scale(.98); }
    87% { opacity: 0; transform: translate3d(0,0,0) scale(.98); }
    94%, 100% { opacity: 1; transform: translate3d(0,0,0) scale(1); }
  }
  @keyframes rg-sync-source-slot {
    0%, 22%, 36%, 100% { border-color:transparent; background:transparent; }
    24%, 35% { border-color:#594035; background:rgba(255,255,255,.012); }
  }
  @keyframes rg-sync-target-slot {
    0%, 18%, 87%, 100% { border-color:#332a26; background:transparent; box-shadow:none; }
    24%, 35% { border-color:#9a4b27; background:rgba(255,80,8,.055); box-shadow:inset 0 0 1.4rem rgba(255,73,0,.08); }
    36%, 86% { border-color:transparent; background:transparent; box-shadow:none; }
  }
  @keyframes rg-sync-local { 0%, 32%, 72%, 100% { opacity:0; transform:translateY(.3rem); } 38%, 64% { opacity:1; transform:translateY(0); } }
  @keyframes rg-sync-packet { 0%, 43%, 57%, 100% { opacity:0; left:0; } 45% { opacity:1; left:0; } 53% { opacity:1; left:100%; } 55% { opacity:0; left:100%; } }
  @keyframes rg-sync-mobile-packet {
    0%, 43% { opacity: 0; stroke-dashoffset: 1; }
    45% { opacity: 1; stroke-dashoffset: 1; }
    53% { opacity: 1; stroke-dashoffset: 0; }
    55%, 100% { opacity: 0; stroke-dashoffset: 0; }
  }
  @keyframes rg-sync-phone-glow { 0%, 52%, 74%, 100% { border-color:#55443b; box-shadow:0 1.8rem 3.5rem -2rem #000, 0 0 2.8rem -2rem rgba(255,76,0,.3); } 57%, 64% { border-color:#9b4b29; box-shadow:0 1.8rem 3.5rem -2rem #000, 0 0 2.8rem -.9rem rgba(255,76,0,.42); } }
  @keyframes rg-sync-row { 0%, 54%, 76%, 100% { border-color:#2b2521; background:#12100e; } 58%, 67% { border-color:#874322; background:#1b110c; box-shadow:0 0 1.5rem -.8rem rgba(255,77,8,.65); } }
  @keyframes rg-sync-state-a { 0%, 56%, 86%, 100% { opacity:1; } 57%, 85% { opacity:0; } }
  @keyframes rg-sync-state-b { 0%, 56%, 86%, 100% { opacity:0; } 57%, 85% { opacity:1; } }
  @keyframes rg-sync-count-a { 0%, 35%, 86%, 100% { opacity:1; } 36%, 85% { opacity:0; } }
  @keyframes rg-sync-count-b { 0%, 35%, 86%, 100% { opacity:0; } 36%, 85% { opacity:1; } }
  @keyframes rg-sync-check { 0%, 56%, 86%, 100% { opacity:0; transform:scale(.6); } 60%, 82% { opacity:1; transform:scale(1); } }
  @keyframes rg-sync-status { 0%, 52%, 86%, 100% { opacity:.35; } 58%, 76% { opacity:1; color:#bc8b70; } }

  @media (max-width: 50rem) {
    .rg-sync-scene { grid-template-columns: minmax(0,1fr) 2.5rem 10.5rem; min-height: 25rem; padding: 1rem; }
    .rg-sync-phone-shell { min-height: 22rem; border-radius: 1.65rem; }
    .rg-sync-column { min-height: 12rem; }
    .rg-sync-task { min-height: 3.75rem; }
    .rg-sync-local-write, .rg-sync-presence { display:none; }
  }

  @media (max-width: 38rem) {
    .rg-sync-scene { grid-template-columns: minmax(0,1fr); gap: 1rem; }
    .rg-sync-transfer {
      position: absolute;
      z-index: 4;
      top: 20.5rem;
      right: 11.5rem;
      left: 3.5rem;
      display: block;
      height: 3rem;
      pointer-events: none;
    }
    .rg-sync-transfer .rg-sync-link { display: none; }
    .rg-sync-transfer .rg-sync-mobile-link { display: block; width: 100%; height: 100%; overflow: visible; }
    .rg-sync-mobile-link path { fill: none; vector-effect: non-scaling-stroke; }
    .rg-sync-mobile-link .track { stroke: #563426; stroke-width: 1; }
    .rg-sync-mobile-link .packet {
      opacity: 0;
      stroke: #ff6500;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-dasharray: .08 .92;
      stroke-dashoffset: 1;
      filter: drop-shadow(0 0 .3rem #ff4a00) drop-shadow(0 0 .65rem rgba(255,74,0,.7));
      animation: rg-sync-mobile-packet var(--sync-cycle) ease-in-out infinite;
    }
    .rg-sync-transfer small { position: absolute; left: .55rem; bottom: -.8rem; }
    .rg-sync-phone { z-index: 5; width: 10.5rem; margin-top: -7rem; margin-inline-start: auto; }
    .rg-sync-browser { margin-inline-end: 2rem; }
    .rg-sync-column { min-height: 10.5rem; }
    .rg-sync-column:first-child { display:none; }
    .rg-sync-columns { grid-template-columns: repeat(2,minmax(0,1fr)); }
    .rg-sync-task {
      top: 3.39rem;
      left: calc(.58rem + 1px);
      width: calc(50% - 1.435rem - 2px);
      height: 4.25rem;
      min-height: 4.25rem;
      box-sizing: border-box;
    }
    @keyframes rg-sync-drag {
      0%, 17% { opacity:1; transform:translate3d(0,0,0) scale(1); }
      23% { opacity:1; transform:translate3d(0,-.2rem,0) scale(1); }
      36%, 80% { opacity:1; transform:translate3d(calc(100% + 1.71rem + 2px),3.27rem,0) scale(1); border-color:#8c4828; box-shadow:0 0 1.8rem -.8rem rgba(255,77,8,.55); }
      86% { opacity:0; transform:translate3d(calc(100% + 1.71rem + 2px),3.27rem,0) scale(1); }
      87% { opacity:0; transform:translate3d(0,0,0) scale(1); }
      94%, 100% { opacity:1; transform:translate3d(0,0,0) scale(1); }
    }
    .rg-sync-demo figcaption { flex-direction:column; }
  }

  @media (prefers-reduced-motion: reduce) {
    .rg-sync-task { transform:translate3d(calc(100% + 1.83rem),2.87rem,0); animation:none; }
    .rg-sync-drop-slot, .rg-sync-local-write, .rg-sync-link i, .rg-sync-mobile-link .packet, .rg-sync-phone-shell, .rg-sync-mobile-row.changing, .rg-sync-mobile-row > b, .rg-sync-phone-status { animation:none; }
    .rg-sync-drop-slot { border-color:transparent; }
    .rg-sync-local-write, .rg-sync-link i { display:none; }
    .rg-sync-mobile-state em:first-child, .rg-sync-changing-count i:first-child { opacity:0; }
    .rg-sync-mobile-state em:last-child, .rg-sync-changing-count i:last-child, .rg-sync-mobile-row > b { opacity:1; }
  }
</style>
