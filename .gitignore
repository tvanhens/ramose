node_modules/
.alchemy/
.astro/
dist/
*.log
.env
.env.*
FABLE_TASK.md

# Generated publish artifacts (see scripts/build-packages.ts): LICENSE and
# NOTICE are copied from the repo root on every build, so the root copies stay
# the single source of truth. The package README is hand-written — it is the
# npm landing page — and is committed.
packages/*/LICENSE
packages/*/NOTICE

# Claude Code scratch. `worktrees/` holds real git worktrees — committing one
# would commit a checkout of another branch. Shared config (settings.json,
# agents/, skills/, commands/) is deliberately NOT ignored so it can be checked in.
.claude/worktrees/
.claude/settings.local.json

# Incremental typecheck cache (tsconfig.json sets `incremental: true`). Machine
# -local and ~2MB — rebuilt on demand by `bun run typecheck`.
*.tsbuildinfo
